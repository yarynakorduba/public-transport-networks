{
  "name": "public_transport_networks",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "frontend": "cd frontend && yarn start",
    "backend": "cd backend && yarn start",
    "start": "concurrently - kill-others \"yarn backend\" \"yarn frontend\"",
    "installBackend": "cd \"backend\" && yarn",
    "installFrontend": "cd \"frontend\" && yarn",
    "installAll": "yarn && yarn installBackend && yarn installFrontend"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "dependencies": {
    "concurrently": "^4.1.0",
    "graphql": "^14.2.1"},
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "node-sass": "^4.11.0",
    "prettier": "^1.16.4"
  }
}
